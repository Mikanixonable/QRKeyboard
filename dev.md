
# 開発ノート

このマークダウンは人間のみが書く

## チケット管理

### done

- サイト幅

- QR, MicroQR, rMQRの選択肢のボタンをリッチにする。
    - ボタンを今の二倍程度厚みがあるものにする。サブスクのプランを選ぶときのような、タイトルにアイコンと説明文がついたもの。
    - ボタン右部には、それぞれの規格でのQRコードのおおまかな見た目、シルエットがわかるアイコンをつける
    - ボタン下部には、その規格の説明があり、埋め込み可能な情報量、形状、そのほかのスペックが書かれている
- QRコード表示部のアスペクト比は常に正方形になるようにする

- DataMatrix, Aztec, バーコードのボタンも同様にリッチにする

- Vericodeの機能を廃止
- titleと題名を「QRKeyboard」に変更
- コードの種類を選択するUIをコンテンツゾーン上部に設けて
- QRコードが表示されるエリアのアスペクト比は正方形にして
- サイトのコンテンツ領域を横に三つに分割して。左から順に左メニューゾーン、中央コンテンツゾーン。右メニューゾーンと呼ぶことにする。左メニューゾーンの上部にサイトタイトルを表示。コンテンツゾーンにこれまでの内容が表示される。
- バーコード機能について。一般に商品のバーコードはバーコードの下に数字があると思うが、下に文字を表示するかどうか切り替えられるようにして。

- 「クリックでモジュールを反転できます」の文言を削除
- 表示域の変更
    - 全てのコードについて、QR表示域の上下左右の中央に表示されるようにする。
    - コードの規格やスペックを変えても表示域の大きさは変わらないようにする
    - デフォルトでは、規格で定められたクワイエットゾーンを白で、クワイエットゾーンを外れた領域を薄い灰色で表示する。両方を同じ色にできる選択肢を作る。また、これらの色は今までと同様ユーザーが選択できるようにもする
    - コード端からQR表示UIの端までの距離(最低値)は、全ての規格で統一する

- エンコード内容の情報量が現在選択している規格、条件をオーバーする場合、複数のコードを表示して対応する。この際、QR表示域の大きさは変更せず、各コードを小さくして配置する。

- 複雑度の選択UIの変更
    - 位置を右メニューゾーンに移動
    - 複数のパラメーターによって決まる選択肢(縦幅、横幅など)は、モーダルを表示し、二次元的に配置されたタイルによって選択できるようにする。この際選択肢の配置は、上方向、右方向に単純、小さい選択肢がくるように配置する。


- コード下にエンコード内容を表示するか否かを選べるようにする。同時に「内容」は廃止する。QRコードが複数表示される場合、各コードの下に該当箇所を表示するか、全体の下に一つだけ表示するか、両方表示するか選べるようにする。ただしStructured Appendの場合各コードの下に表示するものがないと思うので全体の下に一つだけ表示するようにする
- 題名左にfaviconと同様のアイコンを表示

- デコード機能を追加し、カメラ入力や画像選択によってQRコードを読み取れるようにする
    - 現在選択可能な全てのコード形式でコード機能をつける
    - UIは、中央中QRコード表示部を兼用する。
    - 取り込んだQRコードの背景色と本体色は、取り込んだカメラや画像でのQRコードの平均色を採用する。

- 内容表示について
    - 現在クワイエットゾーンに表示されてしまっている、クワイエットゾーン下に表示して
    - 内容がクワイエットゾーンより長くなれば改行して
- 諸元のエンコード内容も、枠をはみ出ている、オーバーしたら改行して
- 現在選択しているコード規格、エンコード内容、色、複雑度、誤り訂正強度、マスクパターン、分割方式を、url変数として格納し、リロード後も維持されるようにして
- 今後の機能追加や変更の作業が少ないトークンで行いやすいように、リファクタリング、バグ抽出、あるいは不自然なポイントの抽出を行いたい
    - このアプリのロジックを再検討して、現状の機能を果たすために不必要な複雑性がある部分を解消して
    - 機能の変化を伴うが、一般的に不自然な実装となっている箇所、または単に不自然な箇所は、それを修正すべきかどうか判断するため、説明して
    - バグを抽出して
 - リファクタリングしたい。

- サイトタイトルのミニアイコンを復活させ、規格選択ボタンのアイコンを削除して
- 複雑度の「自動」をオフにして手動で複雑度を設定できないので、オフにできるようにして

- 「データ」の縦幅がおかしい
- 画像の保存、クリップボードへのコピーを「保存」グループとしてまとめ、ボタン文言と順番は「SVG」「PNG」「コピー」にして。
- Micro QR/rMQRデコード機能を追加して

モバイル版修正
- 規格の選択をプルダウンにし縦幅を節約
- 「カメラで読み取り」「画像を選択」を「デコード」としてプルダウンに格納し、規格の選択プルダウンの右に設置。説明書きはプルダウン内にバインド

- Micro QR/rMQRのデコード非対応: ZXingがこの2規格に対応していないため、生成はできても読み取りができない(ユーザーへの明示的な注意書きもまだない)
- PDF417やGS1 DataBarなど他の1次元/2次元規格は未対応
- クリップボードへのコピー: PNG/SVG保存はあるが、画像をそのままクリップボードにコピーする機能がない

以下の修正を行って
- 「内容表示」チェックボックスを中央上、色選択と同じエリアに移動
- 「容量超過時の分割方式」を右メニューに移動
- 「複雑度」の「自動」チェクボックスは、マスクパターンの「自動」ボタンと同じように、選択肢の上にあるボタンとして実装するように変更
- 現状規格選択UIは題名に付属したミニアイコンと、ボタン右部のアイコンとアイコンが二つある。題名部のミニアイコンを削除して。
- 一次元バーコードの説明文がロゴとかぶっている、他のボタンはアイコンに被っていないので、他ボタンと同じよう改行位置を3文字程度左にずらして
作業後は結果を日本語で回答して

- 保存機能で保存したりコピーしたりする内容は、現在はクワイエットゾーンの範囲になっているが、背景(テキスト表示時ならテキスト入り)を含めてコピーできる選択肢を設けて

- QRの文字入れの位置について、文字の高さは、現在クワイエットゾーン下端のすぐ下に表示されているが、クワイエットゾーン下端とQR表示域下端の中間に位置するように修正して

- 保存UIはQRスペック下

- 「自動」UIがチェックボックスになっていたりボタンになっていて統一性がない
- エンコード内容の入力は中心軸上に欲しいかも、QRコードの下の方がキーボード感は出る？


- モバイルでスクロールフリーになっていないのでスクロールフリーにして
文字入れはWEBUI上の表示と保存後の位置が違う、原因は何だろうか。ちなみにどちらも余白の上下中心からは外れている

- 文字入れの位置が規格によって違うのはなぜ
- バーコードにも文字入れ機能、背景を含めた保存機能を追加
- 背景を含めた保存では、現在はクワイエットゾーンとテキスト表示部のみが保存されているが、実際にWEBUI上で表示されている正方形のQR表示域全体をそのままダウンロードできるようにしてほしい
- 画像保存で、一部の条件ではQRコードのピクセルの境界に灰色の格子状の線が入る、原因を調査

- QRの文字入れの位置について、文字の高さは、QRコード表示域の下の余白の中間に表示されるように修正して。前回修正を行ったが、現在も修正ができていないので修正
- 入力欄削除ボタンの形状を丸に

- 保存範囲の設定のボタンがデカすぎる、チェックボックスやトグルにしても良い、モバイル版は縦節約
- 規格選択UI、QRコードの選択肢のみ枠に色がついていない、ボタン色と同じ青い枠で着色して
- コードの下に文字を表示する、デフォルトオフ







### todo


- 一方で、生成したシンボルをこのアプリの画像デコード機能(ZXing)で読み戻そうとすると失敗することが分かりました。深く調査した結果、これは**当方のエンコーダの誤りではなく、ZXing-js側のfinder pattern探索ロジックの癖(バージョン依存の非互換)**であると判断しました(zint生成のPNGはZXingで正常に読み取れる一方、数学的に同一のパターンでもfinder位置の探索アルゴリズムが特定の版で不安定という状況を確認)。そのため、MicroQR/rMQRと同様に「生成はできるが読み取りは非対応」という注意書きをUIに追加しています。

- 背景色追従
    - 「同じ色にする」ボタンをやめ、クワイエットゾーンに表示on offの機能をつける
- 複雑度のUIが操作しにくい、手動で一段階でも操作できるようにする
- QRコードの複数表示について、無駄が多い
    - 長方形のタイプがスタックされていない→表示領域正方形なら現状の方が美しいのでは
    - 背景は１ピクセル単位であって欲しい、またはクワイエットゾーン確保なら隣接させてもいい
- 内容表示の改行、完全ではない。改行が反映されないなど、Unicode Art突っ込むとそれが表示されるようになってほしい
- 配色プリセット欲しい、nike的雰囲気
- 初期placeholderを別のリンクにしてもいいかも
- 英語化
- アイコンが不正確
- このアプリに不要と思う機能、廃止した方が良いと思う機能を教えて
- 編集機能を強化、お絵描きしたらその内容に合わせて読み取った内容も変化してほしい
- 容量超過時の分割方式は超過時のみ表示？
- ニューモーフィズムデザインにする
- MicroQR・rMQRのデコードで、斜め45度の読み取りができない
- テスト作りたい
    自動テストが皆無: qrcode.js/datamatrix.js/aztec.js/barcode1d.jsのエンコード/デコードロジックに対する単体テストが一つもなく、リグレッションを機械的に検知できない

- 実装コードが増えたら規格の説明がいる。

- デコード時の背景色は、平均色を用いているが、コード色を含めて平均化されるためコントラストが落ちる。そこで、コードの背景色を抽出して背景色として
- 背景色が暗色、コードが明色の時にデコードできないのでできるよう修正
- コード下に文字を表示するか否かをurl変数に含めて永続化
- QR表示域を通常の画像にしたい。右クリック/長押しすると画像として保存できるようにして
- モバイル版文字入れ修正、保存されるコードと、画面上に表示されている文字の大きさが一致しない。画面上にも、保存される内容と同じ大きさの文字を表示して

以下のコードのエンコード、デコードの実装可能性を調査
    - 二次元コード: Han Xin Code, JAB Code, Boxing barcode, DotCode
    - 二次元(極座標系)コード: MaxiCode, ShotCode,
    - 一次元コード: Automatic Car Identification, Code 11, Code 39, Code 93, Code 128, CodabarEuropean Article Number, GS1 DataBar, Industrial 2 of 5Interleaved 2 of 5, ITF-14, Matrix 2 of 5, MSI Barcode, Patch Code, Pharmacode, PlesseyTelepen, UPC
以下のコードのエンコード、デコードの実装可能性を調査
- 二次元コード: Aztec Rune, Code One, Dot Code, Grid Matrix, 
- 複線式バーコード: CODABLOCK F, Code 49, MicroPDF417, PDF417


Aztec Rune, CODABLOCK F, PDF417, UPC-A/UPC-E, Code 93, Codabar, ITF/ITF-14, Industrial・Matrix 2 of 5, MSI, Plessey, Pharmacode, Code 11

- PWA対応して
- 2つ必要な条件、3つ必要な条件も色分けしたい
色分けの説明が欲しい
- マスクパターンはアイコンが欲しい